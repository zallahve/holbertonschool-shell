# Shell, init files, variables and expansions

This project contains scripts that demonstrate shell initialization files, variables, expansions, arithmetic operations, and aliases.

## Files

- **0-alias**: Creates an alias named `ls` that runs `rm -f *`.
